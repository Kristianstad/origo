# Set in stage2:
# ---------------------------------------------------------
# set -e +a +m +s +i -f
# isFirstRun
# VAR_*
# All functions in /start/functions

if [ "$isFirstRun" == "true" ]
then
  htpasswd -bc /etc/nginx/.htpasswd "$VAR_ADMUSER" "$VAR_ADMPASSWORD"
  echo 'generated /etc/nginx/.htpasswd'
  sleep 1
fi
